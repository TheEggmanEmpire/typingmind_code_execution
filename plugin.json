{
  "uuid": "fa4fdbb3-3f30-49d4-9171-40480971642d",
  "id": "fa4fdbb3-3f30-49d4-9171-40480971642d",
  "emoji": "🧪",
  "title": "Code Runner",
  "iconURL": "https://custom.typingmind.com/assets/plugins/javascript.webp",
  "authenticationType": "AUTH_TYPE_NONE",
  "oauthConfig": null,
  "userSettings": [
    {
      "name": "corsProxy",
      "label": "CORS proxy (optional)",
      "description": "Used only when a direct request from Python/JavaScript is blocked by the browser (CORS). Prefix, e.g. https://corsproxy.io/?url= or use {url} as placeholder. Requests that fall back to it are visible to that proxy.",
      "placeholder": "https://corsproxy.io/?url=",
      "required": false
    }
  ],
  "dynamicContextEndpoints": [],
  "implementationType": "javascript",
  "openaiSpec": {
    "name": "run_code",
    "description": "Execute code and return its printed output. Languages: python, javascript, sql (SQLite), c, c++, rust, go, ruby, java, csharp, haskell, lua. Use whenever a result should be computed, not guessed. Always print the values you want returned. INTERNET (python, javascript): the runtime is WASM in the browser tab - no container, no sockets, no DNS; socket/ping/DNS probes always fail. HTTP works via the browser: python `import requests; print(requests.get(url).text)` (also urllib.request.urlopen, `await pyodide.http.pyfetch(url)`); javascript `const r = await fetch(url); console.log(await r.text())`. Browser CORS applies; a blocked request retries through the CORS proxy from plugin settings if set. TEMP FILES: /workspace is an in-memory directory shared by python (cwd), javascript (fs helpers) and sql; persists across calls until page reload. PACKAGES: extra Python packages via `packages` (micropip, session-only). Compiler Explorer languages: no internet, no files. Java: do NOT declare the class public.",
    "parameters": {
      "type": "object",
      "properties": {
        "language": {
          "type": "string",
          "enum": [
            "python",
            "javascript",
            "sql",
            "c",
            "c++",
            "rust",
            "go",
            "ruby",
            "java",
            "csharp",
            "haskell",
            "lua"
          ],
          "description": "Language to execute the code in."
        },
        "code": {
          "type": "string",
          "description": "Complete source code. Must print its own output. Temp files: python starts in /workspace, so open('data.csv','w') writes there and later calls can read it; use /tmp for throwaway files. javascript has `await fs.writeFile(name, textOrBytes)`, `await fs.readFile(name)` (`'binary'` as 2nd arg for bytes), `fs.appendFile`, `fs.readdir('/workspace')`, `fs.exists`, `fs.unlink`, `fs.mkdir`, `fs.stat`, plus `storage.set(key, value)` / `storage.get(key)` for plain values between calls. sql keeps its tables between calls; python can open the same database with `sqlite3.connect('/workspace/data.sqlite')` and sql sees python's committed writes. Delete data.sqlite to reset it. Internet: python `requests.get/post`, `urllib.request.urlopen`, `await pyodide.http.pyfetch`; javascript `fetch`. Python packages can also be installed inline: `import micropip; await micropip.install('pkg')` (top-level await is allowed)."
        },
        "packages": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Python only. Extra PyPI packages to install with micropip before running, e.g. ['beautifulsoup4', 'pyyaml']. Installs into the in-browser runtime for the current session only (temporary, gone on page reload; already-installed packages are skipped on later calls). Works for pure-Python wheels and for packages Pyodide ships prebuilt (numpy, pandas, scipy, scikit-learn, matplotlib, pillow, lxml, sqlalchemy, ...). Packages needing native code not in Pyodide cannot be installed. numpy, pandas, scipy, requests, matplotlib etc. auto-load from imports without listing them here. Slow - omit unless genuinely needed."
        }
      },
      "required": [
        "language",
        "code"
      ]
    }
  },
  "outputType": "respond_to_ai",
  "overviewMarkdown": "## Code Runner (multi-language)\n\nRuns code in 12 languages and returns the output to the AI. No API key. No server.\n\n- **python** - Pyodide (WASM, in-browser). numpy/pandas auto-load from imports; other PyPI packages via `packages` (see below).\n- **javascript** - runs in the plugin sandbox.\n- **sql** - SQLite via sql.js (WASM, in-browser).\n- **c, c++, rust, go, ruby, java, csharp, haskell, lua** - Compiler Explorer (godbolt.org) public API.\n\nFirst Python call takes ~5-15 s while the runtime downloads. Java: do not declare the class `public`.\n\nToolchains (Compiler Explorer): gcc 14.2, rustc 1.82, Go 1.26, Ruby 4.0, JDK 25, .NET 9 (Mono), GHC 9.8, Lua 5.5.\n\n### Python packages\n\n- numpy, pandas, scipy, matplotlib, requests and other packages Pyodide ships auto-load from `import` lines.\n- Anything else: pass `packages: [\"beautifulsoup4\", \"pyyaml\"]` (micropip) or install inline with\n  `import micropip; await micropip.install(\"pkg\")`.\n- Installs are temporary: they live in the in-browser runtime for the session and vanish on page reload.\n- Pure-Python wheels and Pyodide-prebuilt packages only. Packages with native code that Pyodide does not build cannot be installed.\n\n### Internet access\n\n- **python**: `requests`, `urllib.request` (via pyodide-http) and `pyodide.http.pyfetch` all work.\n- **javascript**: `fetch` works.\n- Requests go straight from the browser, so the target must allow CORS. If it does not, set the optional\n  **CORS proxy** plugin setting (e.g. `https://corsproxy.io/?url=`, or any URL containing `{url}`).\n  The proxy is only used for requests that failed directly. Traffic through it is visible to the proxy operator.\n- There is no container behind the runner: Python runs as WASM inside the browser tab. Raw sockets, DNS and ping\n  do not exist there. Emscripten hands out fake `172.29.x.x` addresses and every `connect()` fails with\n  `Host is unreachable`, so socket-level probes prove nothing. HTTP through the browser is the only path.\n- Compiler Explorer languages run in godbolt's sandbox: no network.\n\n### Temporary storage\n\n`/workspace` is an in-memory (WASM MEMFS) scratch directory that lives until the page reloads.\nIt is shared by all three in-browser runtimes:\n\n- **python** starts in `/workspace`; anything written there is available in later calls.\n- **javascript** gets `fs` (`await fs.readFile / writeFile / appendFile / readdir / exists / unlink / mkdir / stat`)\n  on the same directory, plus `storage.get/set/has/delete/keys/clear` for plain values.\n- **sql** keeps its database across calls. Once Python has loaded, the database is mirrored to\n  `/workspace/data.sqlite`, so `sqlite3.connect('data.sqlite')` in Python sees the same tables and SQL sees Python's writes.\n  Deleting `data.sqlite` resets the SQL database.\n\nExample: *Use run_code to compute the first 20 primes in Rust.*\n"
}
